services:

  backend:

    build: ./backend

    container_name: complaint_backend

    ports:
      - "8000:8000"

    env_file:
      - ./backend/.env

  frontend:

    build: ./frontend

    container_name: complaint_frontend

    ports:
      - "5173:5173"

    depends_on:
      - backend